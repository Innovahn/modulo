# -*- encoding: utf-8 -*-
from openerp import models, fields, api
from datetime import datetime, date

class sale_type_entrega(models.Model):
    _name="sale.entrega"
    name=fields.Char("Tipo de entrega")



